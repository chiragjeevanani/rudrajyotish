PORT=5000
MONGO_URI=mongodb://localhost:27017/achyutam
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# Credentials for the sole administrator
ADMIN_USERNAME=admin
ADMIN_PASSWORD=achyutam@admin123

# Razorpay credentials
RAZORPAY_KEY_ID=rzp_test_SatrrxFwKXJX8e
RAZORPAY_KEY_SECRET=2qg7F437K2Oqq7gVRH41NZen

# SMTP configurations (Nodemailer)
# For Gmail: SMTP_USER is your full Gmail address and SMTP_PASS is a 16-char
# "App Password" (NOT your normal password). Create one at
# https://myaccount.google.com/apppasswords (requires 2-Step Verification).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Business inbox that receives a notification for every booking.
# Supports a comma-separated list. Falls back to SMTP_USER if left empty.
ADMIN_EMAIL=admin@achyutam.com

# Client URLs
CLIENT_URL=http://localhost:5173
